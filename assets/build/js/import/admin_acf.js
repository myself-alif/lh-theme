(function ($) {
    $(function () {
        // const $body = $('body');

        // Set theme colours palette based in ACF color picker fields
        // /* eslint-disable-next-line no-undef */
        // if (typeof acf !== 'undefined' && acf.add_filter) {
        //     /* eslint-disable-next-line no-undef */
        //     acf.add_filter('color_picker_args', function (args) {
        //         args.palettes = ['#122550', '#1e3059', '#7dca5a', '#4b2a3c', '#543546', '#e461a3', '#36453b', '#404e45', '#e42f2f', '#de541e', '#e05d29', '#ffb30f'];

        //         return args;
        //     });
        // }
    });
})(jQuery);
